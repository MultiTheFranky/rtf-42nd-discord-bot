version: "3.8"
services:
  rtf-42nd-discord-bot:
    build: .
    image: multithefranky/rtf-42nd-discord-bot
    platform: linux/amd64
    container_name: rtf-42nd-discord-bot
    volumes:
      - "./log:/app/log"
      - "./db:/app/db"
    env_file: .env
    ports:
      - "${PORT}:${PORT}"
    restart: unless-stopped
