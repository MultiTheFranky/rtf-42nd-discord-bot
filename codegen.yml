overwrite: true
schema: "src/graphql/schemas/**.graphql"
documents: null
generates:
  src/graphql/__generated__/graphql.ts:
    plugins:
      - "typescript"
      - "typescript-resolvers"
    config:
      scalars:
        Date: Date
